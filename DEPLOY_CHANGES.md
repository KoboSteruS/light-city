# 🔧 Изменения для деплоя на Render.com

## ✅ Что исправлено:

### 1. **requirements.txt**
- ✅ Обновлен Pillow с `==10.1.0` на `>=10.4.0` (совместимость с Python 3.12+)
- ✅ Заменены фиксированные версии на минимальные (>=) для гибкости
- ✅ Все пакеты обновлены до совместимых версий

### 2. **config/settings.py**
- ✅ Добавлена поддержка PostgreSQL через `dj-database-url`
- ✅ Автоматическое определение БД: PostgreSQL на Render, SQLite локально
- ✅ Исправлены настройки логирования для production (stdout для Render)
- ✅ Добавлено создание папки `logs/` если её нет

### 3. **runtime.txt**
- ✅ Создан файл с указанием Python 3.12.7 (стабильная версия)

### 4. **.gitignore**
- ✅ Добавлена папка `logs/` в игнор

### 5. **Документация**
- ✅ Создан `RENDER_DEPLOY.md` - подробная инструкция
- ✅ Создан `RENDER_SETTINGS.txt` - шпаргалка с настройками

## 📋 Что нужно сделать в Render:

1. **Обновить Build Command:**
   ```
   pip install --upgrade pip && pip install -r requirements.txt
   ```

2. **Обновить Start Command:**
   ```
   gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120
   ```

3. **Добавить Pre-Deploy Command (опционально):**
   ```
   python manage.py migrate --noinput && python manage.py collectstatic --noinput
   ```

4. **Установить Environment Variables:**
   - `SECRET_KEY` - новый ключ для production
   - `DEBUG=False`
   - `ALLOWED_HOSTS=light-city.onrender.com`
   - `DATABASE_URL` - автоматически от PostgreSQL сервиса Render

## 🚀 После деплоя:

1. Создай PostgreSQL базу данных в Render (если еще нет)
2. Подключи DATABASE_URL к Web Service
3. Выполни миграции через Render Shell:
   ```bash
   python manage.py migrate
   python manage.py createsuperuser
   python manage.py create_demo_data
   python manage.py load_catalog_demo
   python manage.py create_demo_portfolio
   ```

## ⚠️ Важно:

- На бесплатном плане Render файлы в `/media/` будут теряться при перезапуске
- Для production используй S3 или другой cloud storage для медиа-файлов
- После первого деплоя проверь логи на ошибки

