# Яркий Город - Рекламное агентство

![Python](https://img.shields.io/badge/python-3.10+-blue.svg)
![Django](https://img.shields.io/badge/django-4.2-green.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

Современный, защищенный сайт рекламного агентства "Яркий Город" с административной панелью и системой управления контентом.

## 🚀 Особенности

- ✅ **Django 4.2** - надежный и проверенный фреймворк
- ✅ **Защищенная админка** - кастомный URL, защита от брутфорса (Django Axes)
- ✅ **Модульная архитектура** - разделение на приложения по функционалу
- ✅ **BaseModel** - UUID primary keys, автоматические timestamps
- ✅ **Логирование** - Loguru для профессионального логирования
- ✅ **Адаптивный дизайн** - Bootstrap 5 + кастомные стили
- ✅ **Слайдер** - Swiper.js для красивых презентаций
- ✅ **CKEditor** - визуальный редактор контента
- ✅ **Оптимизация изображений** - sorl-thumbnail
- ✅ **Production-ready** - настроено для продакшена

## 📋 Требования

- Python 3.10+
- pip
- virtualenv (рекомендуется)
- SQLite (для разработки) или PostgreSQL (для продакшена)

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd Yrkiy_gorod
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта (скопируйте `.env.example`):

```env
DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# Кастомный URL для админки (для безопасности)
ADMIN_URL=secret-admin-panel/

# Database (для PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/yarko_gorod

# Email
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
```

### 5. Применение миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### 6. Создание суперпользователя

```bash
python manage.py createsuperuser
```

### 7. Сбор статических файлов

```bash
python manage.py collectstatic --noinput
```

### 8. Создание необходимых директорий

```bash
# Windows
mkdir logs
mkdir media
mkdir media\slider
mkdir media\about
mkdir media\services
mkdir media\portfolio

# Linux/Mac
mkdir -p logs
mkdir -p media/{slider,about,services,portfolio}
```

### 9. Запуск сервера разработки

```bash
python manage.py runserver
```

Сайт будет доступен по адресу: `http://127.0.0.1:8000/`

Админка: `http://127.0.0.1:8000/{ADMIN_URL}` (замените {ADMIN_URL} на значение из .env)

## 📁 Структура проекта

```
Yrkiy_gorod/
├── apps/                      # Приложения Django
│   ├── core/                 # Базовые модели и утилиты
│   │   └── models/
│   │       └── base.py      # BaseModel с UUID
│   ├── main/                # Главная страница
│   │   ├── models/          # Slider, AboutUs, SiteSettings
│   │   ├── views.py
│   │   └── admin.py
│   ├── services/            # Услуги
│   ├── portfolio/           # Портфолио работ
│   └── contacts/            # Контакты и обратная связь
├── config/                  # Настройки Django
│   ├── settings.py         # Главные настройки
│   ├── urls.py             # Маршруты
│   ├── wsgi.py
│   └── asgi.py
├── templates/              # HTML шаблоны
│   ├── base.html          # Базовый шаблон
│   └── main/
│       └── home.html      # Главная страница
├── static/                 # Статические файлы
│   ├── css/
│   │   └── style.css      # Главные стили
│   ├── js/
│   │   └── main.js        # JavaScript
│   └── img/
├── media/                  # Загружаемые файлы
├── logs/                   # Логи приложения
├── requirements.txt        # Зависимости Python
├── manage.py              # Django CLI
└── README.md              # Документация
```

## 🔐 Безопасность

### Реализованные меры безопасности:

1. **Защита админки:**
   - Кастомный URL (настраивается через .env)
   - Django Axes - защита от брутфорса (блокировка после 5 попыток)
   - Сложные пароли (минимум 10 символов)

2. **Защита данных:**
   - UUID вместо автоинкрементных ID
   - CSRF защита
   - XSS защита
   - Secure cookies в продакшене

3. **Валидация:**
   - Валидация всех форм
   - Регулярные выражения для телефонов
   - Защита от SQL-инъекций (ORM Django)

4. **Логирование:**
   - Все действия логируются через Loguru
   - Отдельный файл для ошибок
   - Ротация логов (10 MB)

## 📊 Модели данных

### BaseModel (apps.core.models)
Базовая модель для всех сущностей:
- `uuid` - UUID4 primary key
- `created_at` - дата создания
- `updated_at` - дата обновления

### Slider (apps.main.models)
Слайды на главной странице:
- title, subtitle
- image, button_text, button_link
- order, is_active

### Service (apps.services.models)
Услуги агентства:
- name, icon, description
- order, is_active

### PortfolioItem (apps.portfolio.models)
Работы в портфолио:
- title, category, description
- image, client, date_completed
- is_featured, is_active

### ContactMessage (apps.contacts.models)
Обращения клиентов:
- name, phone, email, message
- status (TextChoices: new, in_progress, completed, cancelled)
- is_read, admin_notes

## 🎨 Дизайн

- **Цветовая схема:**
  - Основной: #F8D12C (желтый)
  - Вторичный: #2C3E50 (темно-серый)
  - Фон: #F8F9FA (светло-серый)

- **Шрифты:** Inter (Google Fonts)
- **Иконки:** Custom SVG
- **Адаптивность:** Mobile-first подход

## 🚀 Деплой на продакшен

### 1. Измените настройки в .env:

```env
DEBUG=False
SECRET_KEY=<генерируйте длинный случайный ключ>
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
CSRF_COOKIE_SECURE=True
SESSION_COOKIE_SECURE=True
SECURE_SSL_REDIRECT=True
```

### 2. Используйте PostgreSQL:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/yarko_gorod
```

### 3. Настройте веб-сервер (Nginx + Gunicorn):

```bash
gunicorn config.wsgi:application --bind 0.0.0.0:8000
```

### 4. Используйте SSL сертификат (Let's Encrypt)

## 🧪 Тестирование

```bash
# Запуск всех тестов
python manage.py test

# Запуск тестов конкретного приложения
python manage.py test apps.main

# С покрытием кода
coverage run --source='.' manage.py test
coverage report
```

## 📝 Лицензия

MIT License

## 👨‍💻 Автор

Яркий Город - Рекламное агентство полного цикла

**Контакты:**
- Телефон: 8 (8142) 28-09-03
- Email: yarko_ptz@mail.ru
- Адрес: г. Петрозаводск, Муезерская улица, 15Ак8

---

**Разработано с ❤️ для профессиональной работы**

