# Настройка Telegram бота для уведомлений о заявках

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install requests
```

### 2. Применение миграций

```bash
python manage.py migrate
```

### 3. Настройка токена бота

1. Откройте файл `.env` в корне проекта
2. Добавьте строку:
   ```env
   TELEGRAM_BOT_TOKEN=8579619332:AAG-ADquy2sin1eYHOZPVB5FQDrpolHCnWI
   ```
3. Сохраните файл

### 4. Запуск бота

```bash
python manage.py telegram_bot
```

Бот будет работать в фоновом режиме и ожидать команду `/start` от руководителей.

### 5. Регистрация руководителей

1. Руководитель должен найти вашего бота в Telegram
2. Отправить команду `/start`
3. Бот автоматически сохранит chat_id и подтвердит подписку

### 6. Проверка работы

После регистрации руководителей, все заявки с сайта (из модального окна "Заказать звонок" и обычной формы) будут автоматически отправляться в зарегистрированные Telegram чаты.

## Управление чатами

В админ-панели Django в разделе **Telegram чаты** можно:
- Просмотреть все зарегистрированные чаты
- Включить/отключить уведомления для конкретного чата (поле `is_active`)
- Увидеть информацию о пользователях (username, first_name)

## Формат уведомлений

Уведомления отправляются в формате:

```
📞 Заказ звонка

👤 Имя: Иван Иванов
📱 Телефон: +79991234567
📧 Email: ivan@example.com

💬 Сообщение:
Заявка с услуги: Оклейка авто

Хочу заказать оклейку
```

## Запуск бота в production

Для production рекомендуется запускать бота как системный сервис или через supervisor. Пример:

```ini
[program:telegram_bot]
command=/path/to/venv/bin/python /path/to/manage.py telegram_bot
directory=/path/to/project
autostart=true
autorestart=true
user=www-data
```

## Устранение проблем

- **Бот не отвечает**: Проверьте, что бот запущен и токен правильный
- **Уведомления не приходят**: Убедитесь, что есть активные чаты (is_active=True) в админке
- **Ошибка при отправке**: Проверьте логи Django для деталей
