# 🚨 ИСПРАВЛЕНИЕ ОШИБКИ 400 НА RENDER

## ❌ Проблема:
```
127.0.0.1 - - [27/Dec/2025:19:34:13 +0300] "GET / HTTP/1.1" 400 143
```

**Ошибка 400 (Bad Request)** - это проблема с `ALLOWED_HOSTS` в Django.

---

## ✅ РЕШЕНИЕ:

### Вариант 1: Автоматическое определение (уже исправлено в коде)

Код теперь автоматически определяет домен Render через переменную `RENDER_EXTERNAL_HOSTNAME`.

**Но нужно убедиться, что в Render Dashboard установлены правильные Environment Variables:**

### Environment Variables в Render Dashboard:

1. **SECRET_KEY** - сгенерируй новый ключ:
   ```bash
   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
   ```

2. **DEBUG** - установи:
   ```
   False
   ```

3. **ALLOWED_HOSTS** - установи (через запятую):
   ```
   light-city.onrender.com,*.onrender.com
   ```
   
   **ИЛИ** просто:
   ```
   light-city.onrender.com
   ```

---

### Вариант 2: Если автоматическое определение не работает

Добавь в Environment Variables:

```
RENDER_EXTERNAL_HOSTNAME=light-city.onrender.com
ALLOWED_HOSTS=light-city.onrender.com,*.onrender.com
```

---

## 📋 Полный список Environment Variables для Render:

```
SECRET_KEY=<твой-новый-секретный-ключ>
DEBUG=False
ALLOWED_HOSTS=light-city.onrender.com,*.onrender.com
```

**База данных:**
- `DATABASE_URL` - Render установит автоматически при создании PostgreSQL

---

## 🔧 Дополнительные настройки (опционально):

Если хочешь видеть детальные ошибки во время отладки:

```
DEBUG=True
ALLOWED_HOSTS=light-city.onrender.com,*.onrender.com
```

**⚠️ ВАЖНО:** После отладки верни `DEBUG=False` для безопасности!

---

## 🚀 После исправления:

1. Сохрани Environment Variables в Render Dashboard
2. Нажми "Manual Deploy" или подожди автоматического деплоя
3. Проверь сайт - ошибка 400 должна исчезнуть

---

## 🐛 Если ошибка 400 все еще есть:

1. Проверь логи в Render Dashboard - там должна быть детальная информация
2. Убедись, что `ALLOWED_HOSTS` содержит точное имя домена без `https://`
3. Попробуй добавить `*.onrender.com` для всех поддоменов Render

---

## 📝 Проверка через Render Shell:

Если хочешь проверить настройки, открой Render Shell и выполни:

```python
python manage.py shell
>>> from django.conf import settings
>>> print(settings.ALLOWED_HOSTS)
>>> print(settings.DEBUG)
```

Должно показать:
```
['light-city.onrender.com', '*.onrender.com']
False
```

