# 📸 Загрузка фотографий в портфолио

## Описание

Management команда для загрузки фотографий из папок в портфолио сайта.

## Поддерживаемые папки

- `авто` → Оклейка авто (категория: Брендирование авто)
- `вывески` → Вывески (категория: Вывески)
- `неон` → Неон (категория: Неон)
- `инт` → Холсты (категория: Печать и полиграфия)

## Использование

### 1. Загрузка одной папки

```bash
python manage.py upload_portfolio_photos /path/to/photos --folder авто
python manage.py upload_portfolio_photos /path/to/photos --folder вывески
python manage.py upload_portfolio_photos /path/to/photos --folder неон
python manage.py upload_portfolio_photos /path/to/photos --folder инт
```

### 2. Загрузка всех папок сразу

```bash
python manage.py upload_portfolio_photos /path/to/photos --all
```

### 3. Загрузка с пропуском дубликатов

```bash
python manage.py upload_portfolio_photos /path/to/photos --folder авто --skip-duplicates
```

## Структура папок

На сервере должна быть следующая структура:

```
/path/to/photos/
├── авто/
│   ├── photo1.jpg
│   ├── photo2.jpg
│   └── ...
├── вывески/
│   ├── photo1.jpg
│   └── ...
├── неон/
│   └── ...
└── инт/
    └── ...
```

## Примеры использования

### Загрузка фоток с локальной машины на сервер

1. **Загрузите фотки на сервер** (через SCP, FTP и т.д.):

```bash
# С локальной машины
scp -r авто вывески неон инт user@server:/tmp/photos/
```

2. **На сервере запустите команду**:

```bash
cd /path/to/project
source venv/bin/activate  # или venv\Scripts\activate на Windows
python manage.py upload_portfolio_photos /tmp/photos --all --skip-duplicates
```

### Загрузка через SSH

```bash
ssh user@server
cd /path/to/project
source venv/bin/activate
python manage.py upload_portfolio_photos /tmp/photos --all --skip-duplicates
```

## Параметры

- `path` - Путь к папке с фотографиями (обязательный)
- `--folder` - Название папки для загрузки (авто, вывески, неон, инт)
- `--all` - Загрузить все папки из маппинга
- `--skip-duplicates` - Пропускать дубликаты (проверка по имени файла)

## Поддерживаемые форматы

- `.jpg` / `.JPG`
- `.jpeg` / `.JPEG`
- `.png` / `.PNG`
- `.webp` / `.WEBP`

## Что делает команда

1. ✅ Находит папку с фотографиями
2. ✅ Определяет категорию и услугу по названию папки
3. ✅ Проверяет дубликаты (если указан `--skip-duplicates`)
4. ✅ Загружает фотографии в `media/portfolio/`
5. ✅ Создает записи в БД с привязкой к услуге и категории
6. ✅ Выводит статистику: создано, пропущено, ошибок

## Пример вывода

```
[*] Обработка папки: авто
[OK] Найдена категория: Брендирование авто
[OK] Найдена услуга: Оклейка авто
[OK] Найдено фотографий: 5
  [OK] Добавлено: photo1.jpg
  [OK] Добавлено: photo2.jpg
  [SKIP] Пропущен дубликат: photo3.jpg
  [OK] Добавлено: photo4.jpg
  [OK] Добавлено: photo5.jpg

==================================================
[OK] Создано записей: 4
[SKIP] Пропущено дубликатов: 1
[ERROR] Ошибок: 0
==================================================
```

## Удаление дубликатов

Если нужно удалить дубликаты после загрузки, используйте команду:

```bash
python manage.py remove_portfolio_duplicates
```

(Эта команда должна быть создана отдельно, если нужно)

## Важные замечания

⚠️ **Всегда используйте `--skip-duplicates`** при повторной загрузке!

⚠️ **Проверяйте путь** перед запуском команды

⚠️ **Делайте бэкап БД** перед массовой загрузкой

## Troubleshooting

### Ошибка: "Категория не найдена"
- Проверьте, что категория существует в БД
- Проверьте правильность названия в маппинге

### Ошибка: "Услуга не найдена"
- Проверьте slug услуги в БД
- Убедитесь, что услуга активна (`is_active=True`)

### Ошибка: "Папка не найдена"
- Проверьте путь к папке
- Убедитесь, что папка существует и доступна для чтения

### Ошибка: "Фотографии не найдены"
- Проверьте расширения файлов (должны быть .jpg, .png и т.д.)
- Убедитесь, что файлы находятся в правильной папке
