# 📞 Модальная форма "Заказать звонок" - Инструкция

## 🎯 Что реализовано

### 1. Универсальная модальная форма
- **Расположение:** Модальное окно в `base.html`
- **ID модального окна:** `#callbackModal`
- **Вызов:** Через атрибут `data-bs-toggle="modal" data-bs-target="#callbackModal"`

### 2. Где работает форма

#### ✅ В шапке сайта (все страницы)
```html
<button type="button" class="btn btn-header-action" 
        data-bs-toggle="modal" data-bs-target="#callbackModal">
    <i class="bi bi-telephone-fill me-2"></i>
    Заказать звонок
</button>
```

#### ✅ На странице услуги (service_detail.html)
```html
<button type="button" class="btn btn-primary btn-lg w-100" 
        data-bs-toggle="modal" data-bs-target="#callbackModal">
    <i class="bi bi-telephone-fill me-2"></i>
    Оставить заявку
</button>
```

---

## 🔥 Умные фишки

### 1. Автоматическое определение контекста
Модалка **автоматически** понимает, откуда её открыли:

**Со страницы услуги:**
- Заголовок: "Заказать [Название услуги]"
- Подзаголовок: "Оставьте свои контакты, и наш менеджер свяжется с вами для уточнения деталей"
- В скрытое поле `service_context` записывается: "Заявка с услуги: [Название услуги]"

**Из шапки (любая страница):**
- Заголовок: "Заказать звонок"
- Подзаголовок: "Оставьте свой номер телефона и мы свяжемся с вами в ближайшее время"
- Контекст услуги не заполняется

### 2. Автоматическая маска телефона
При вводе телефона автоматически форматируется:
```
+7 (900) 123-45-67
```

### 3. Валидация на клиенте
- Имя - обязательно
- Телефон - обязательно
- Чекбокс политики конфиденциальности - обязательно
- Комментарий - необязательно

### 4. AJAX отправка
- Форма отправляется без перезагрузки страницы
- Показывается loader во время отправки
- После успешной отправки - сообщение "Спасибо за обращение!"
- Через 5 секунд модалка автоматически закрывается и форма очищается

---

## 🛠️ Как добавить кнопку вызова модалки

### Пример 1: Обычная кнопка
```html
<button type="button" class="btn btn-primary" 
        data-bs-toggle="modal" data-bs-target="#callbackModal">
    Заказать звонок
</button>
```

### Пример 2: Ссылка
```html
<a href="#" class="callback-link" 
   data-bs-toggle="modal" data-bs-target="#callbackModal">
    Получить консультацию
</a>
```

### Пример 3: Кнопка с иконкой
```html
<button type="button" class="btn btn-success btn-lg" 
        data-bs-toggle="modal" data-bs-target="#callbackModal">
    <i class="bi bi-telephone-fill me-2"></i>
    Позвоните мне
</button>
```

---

## 📋 Обработка на сервере

### View: `ContactFormView` (apps/contacts/views.py)

**Что происходит:**
1. Проверяется чекбокс политики конфиденциальности
2. Из поля `service_context` берётся контекст услуги (если есть)
3. Контекст добавляется в начало сообщения
4. Сообщение сохраняется в БД (`ContactMessage`)
5. Логируется в файл + консоль через `loguru`
6. Возвращается JSON ответ для AJAX

**Пример записи в БД:**
```
Имя: Иван Иванов
Телефон: +7 (900) 123-45-67
Email: (пусто)
Сообщение: 
  Заявка с услуги: Вывески
  
  Хочу заказать вывеску для кафе
```

**Пример лога:**
```
2026-01-19 16:35:00 | INFO | Новое обращение (заказ звонка) от Иван Иванов, телефон: +7 (900) 123-45-67 | Заявка с услуги: Вывески
```

---

## 🎨 Стили модалки

### Основные классы:
- `.callback-modal` - контейнер модалки
- `.callback-modal-header` - шапка с иконкой и заголовком
- `.callback-form` - форма
- `.callback-submit` - кнопка отправки
- `.callback-success` - блок успешной отправки
- `.input-with-icon` - поле ввода с иконкой

### Цвета:
- Иконка телефона: желтый градиент (`var(--primary-color)`)
- Кнопка отправки: желтая с hover эффектом
- Успешная отправка: зеленая иконка `bi-check-circle-fill`

---

## 🔧 Технические детали

### JavaScript логика (base.html)

**1. Определение контекста услуги:**
```javascript
callbackModal.addEventListener('show.bs.modal', function(event) {
    const serviceTitle = document.querySelector('.service-detail-title');
    if (serviceTitle) {
        serviceContextInput.value = 'Заявка с услуги: ' + serviceTitle.textContent.trim();
        modalTitle.textContent = 'Заказать ' + serviceTitle.textContent.trim();
    }
});
```

**2. AJAX отправка:**
```javascript
fetch(this.action, {
    method: 'POST',
    body: formData,
    headers: {
        'X-Requested-With': 'XMLHttpRequest'
    }
})
.then(response => response.json())
.then(data => {
    if (data.success) {
        // Показываем success блок
        // Через 5 секунд закрываем модалку
    }
});
```

---

## 📊 Пример работы

### Сценарий 1: Пользователь на главной странице
1. Нажимает "Заказать звонок" в шапке
2. Открывается модалка с заголовком "Заказать звонок"
3. Вводит имя, телефон, комментарий
4. Нажимает "Отправить заявку"
5. Видит "Спасибо за обращение!"
6. Через 5 секунд модалка закрывается

**В админке:**
```
Имя: Петр Сидоров
Телефон: +7 (911) 222-33-44
Сообщение: Хочу заказать рекламу для магазина
```

### Сценарий 2: Пользователь на странице услуги "Вывески"
1. Нажимает "Оставить заявку"
2. Открывается модалка с заголовком "Заказать Вывески"
3. Вводит имя, телефон
4. Нажимает "Отправить заявку"
5. Видит "Спасибо за обращение!"

**В админке:**
```
Имя: Мария Петрова
Телефон: +7 (922) 555-66-77
Сообщение: Заявка с услуги: Вывески
```

---

## ✅ Checklist для добавления на другие страницы

- [ ] Открыть шаблон страницы
- [ ] Добавить кнопку с атрибутами:
  - `data-bs-toggle="modal"`
  - `data-bs-target="#callbackModal"`
- [ ] Если нужен автоконтекст - добавить элемент с классом `.service-detail-title`
- [ ] Сохранить и протестировать

---

## 🚀 Что можно улучшить

1. **Email уведомления:** Отправлять письмо на почту при новой заявке
2. **Telegram bot:** Дублировать заявки в Telegram
3. **Капча:** Добавить Google reCAPTCHA для защиты от спама
4. **Analytics:** Отслеживать конверсию модалки через Google Analytics/Яндекс.Метрику
5. **A/B тесты:** Тестировать разные тексты кнопок и заголовков

---

**Все готово! Модалка работает на всех страницах! 🎉**
